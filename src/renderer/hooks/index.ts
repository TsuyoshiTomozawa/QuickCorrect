/**
 * QuickCorrect - React Hooks Export
 * 
 * Central export point for all custom React hooks
 */

export { 
  useElectronAPI, 
  useTextSelection, 
  useWindowControls, 
  useClipboard 
} from './useElectronAPI';

export { useCorrection } from './useCorrection';
export { useSettings } from './useSettings';
export { useHistory } from './useHistory';